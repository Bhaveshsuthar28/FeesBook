# API base URL must include /api/v1
VITE_BASE_URL=http://localhost:8000/api/v1

# Student photo and school asset uploads use ImageKit via the backend
# GET /students/imagekit-auth — configure IMAGEKIT_* keys in Backend/.env
